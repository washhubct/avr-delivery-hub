{
  "name": "avr-delivery-hub-functions",
  "description": "Firebase Functions \u2014 Last Mile (AVR Logistic Delivery Hub)",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project avr-logistic-dashboard",
    "copy-core": "cp ../js/fic/fic-core.js ./fic-core.js",
    "test": "npm run copy-core && node --test test/*.test.js"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "googleapis": "^140.0.1",
    "web-push": "^3.6.7"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "private": true
}
